// web-client/src/Integration.jsx
import React, { useState } from 'react';
import { Terminal, Key, Copy, Check, Server, Shield } from 'lucide-react';

const Integration = () => {
  return (
    <div className="animate-fade-in" style={{ color: 'white', textAlign: 'center', marginTop: '50px' }}>
      <h1>Integration Page</h1>
      <p>If you can see this, the routing is working!</p>
    </div>
  );
};

export default Integration;